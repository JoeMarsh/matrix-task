interface:
  display_name: "Matrix Task"
  short_description: "Audit and finish complex tasks completely"
  default_prompt: "Use $matrix-task to complete this task or ticket with a requirements matrix, review-budget ledger, invariant checks, bounded clean-agent reviews, a blocker challenge, evidence, and a final report."
